# Static site: serve from repo root (index.html is the car simulation)
# Explicit no-op build so Netlify does NOT run pip/npm install (which would fail on Python deps)
[build]
  publish = "."
  command = "echo 'Static site — no build required'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
